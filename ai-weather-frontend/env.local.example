## Frontend Environment (Example)
## Copy values into `ai-weather-frontend/.env.local` (do not commit real .env.local).

# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001


